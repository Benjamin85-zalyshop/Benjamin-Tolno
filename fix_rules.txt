rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /schools/{schoolId} {
      allow read, write: if request.auth != null && (
        request.auth.token.email == schoolId ||
        request.auth.token.email == "benjamintolno7@gmail.com"
      );
      
      match /{document=**} {
        allow read, write: if request.auth != null && (
          request.auth.token.email == schoolId ||
          request.auth.token.email == "benjamintolno7@gmail.com"
        );
      }
    }
  }
}
